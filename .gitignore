# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments + local env
.venv/
.env

# Training artifacts (ultralytics runs, local datasets, logs) — never committed
datasets/
exports/
runs/
logs/
*.log

# Model weights: every .pt is ignored except the committed production model
*.pt
!models/best.pt
